# W2-登录页与主布局 UI 缺陷修复评审纪要 v2（二轮微调）

- **日期**：2026-08-21
- **评审子代理**：frontend_experience_engineer（只读会签，定义 `.agents/frontend_experience_engineer.md`）
- **评审对象**：用户 2026-08-21 二轮提报的 4 项登录页微调（v1 纪要见同目录 v1）
- **评审方式**：子代理只读评审＋viewBox 几何脚本复算＋vue-tsc 复跑＋浏览器实测（预览模式）
- **结论**：**通过（可验收）**——无 P0/P1/P2；4 项 P3 均为验证记录口径问题（其中两项实际结果优于声明），已随批修正基线与注释数字记载

## 1. 用户 4 项要求与实现

| # | 用户要求 | 实现（LoginBrandArt.vue） | 验证 |
|---|---------|--------------------------|------|
| 1 | 楼层、车辆和底部保持一点间距 | 场景组 `translate(0,-20)`：楼底 740→720、车轮底 719→699，距地平线（viewBox 底边 742）约 22 单位 | 浏览器实测：车轮距地面带 22.0px、楼距 20.4px（scale≈0.97）✓ |
| 2 | 两条线依次往上平移、和车棚留点间隔 | 能量流双曲线整体上移 55px；原第二条线最深点 (570,470) 恰被雨棚顶 y=470 盖住，上移后最深 y=415.2，与雨棚顶（上移后 450）中心间隔 34.8、含 stroke 半宽 33.5 ≥30 ✓ | 浏览器实测：线 bottom 343.5/403.4 vs 雨棚 top 437.2（间隔 33.8px 起）✓ |
| 3 | 保持现有间距、整体往左平移 50px 左右 | 场景组 `translate(-50,0)`：主体安全区 530-1070→480-1020；1920×1080 全景完整，右楼不与登录卡片冲突（卡片压右楼1顶角约 91×48px 属悬浮卡片纵深构图，评审认定可接受） | 三主题截图确认左移生效 ✓ |
| 4 | 楼层小方框位置合适、保证间隔 | 4 栋楼窗网格对称边距：左楼1 11/11（原右列 1px 贴框）、左楼2 17/17、右楼1 12/11（原右边距 5px）、右楼2 14/15，全部 ≥11 | 数值复算吻合＋三主题截图无贴边 ✓ |

## 2. 评审核查结论（子代理评审摘要）

- 固定核查项：目录落位/令牌与可访问性/vue-tsc 通过（已确认）；路由菜单、权限显隐、状态覆盖、API 边界（纯 SVG 几何调整，不适用/无变更，已确认）；清单-代码一致性（基线四项修订同步到位，已确认）。
- 重点复算：①窗边距四楼全部与声明吻合；②能量线-雨棚间隔 33.5-34.8 ≥30 达标，线2 全线位于雨棚上方；③主体落位 480-1020、底部间距 22-23 单位、投影无越界、渐隐遮罩关系正常；④768×1024 竖屏取舍成立（实际裁切优于声明，见 P3-1）；⑤注释与基线一致性已确认。

## 3. 问题分级与处置

| 级别 | 问题 | 处置 |
|------|------|------|
| P3-1 | 768×1024 裁切量记载口径错误：按容器全高 1024 计算得 ~42px，实际容器 90% 高（921.6px）正确可见区 [490.8,1109.2]，雨棚左端实际仅 ~13 单位裁切（实际优于声明） | **已修正**：基线与组件注释数字更正为 ~13 单位 |
| P3-2 | 验证声明「右楼与卡片无重叠」坐标系混用（viewBox 单位 vs CSS px）；实际 1920 主视口卡片左缘（viewBox≈1120.6）与右楼1 存在 ~91×48 单位交叠——属悬浮卡片压背景的常规纵深构图，无需修改 | **记载于本纪要**（验证记录更正）；如用户后续反馈观感再调右楼位置 |
| P3-3 | 右楼窗边距 12/11、14/15 近似对称（差 1 单位≈1.3px 不可辨），满足 ≥11 硬性要求 | 不改（后续顺手取整即可） |
| P3-4 | 768×1024 下左右氛围楼整体出画，基线未明确记载 | **已补记**：基线补「左右氛围楼整体出画属可接受取舍」 |

## 4. 验证证据

- vue-tsc 通过；三主题浏览器截图（场景左移、底部留距、线悬浮、窗边距均匀）。
- 浏览器实测几何：车轮底 679.1/697.9、楼底 699.5、地面带 top 719.9（间距 22.0/20.4px）；能量线 bottom 343.5/403.4 vs 雨棚 top 437.2。
- 子代理独立复算：贝塞尔极值（线2 最深 y=415.20@x≈582）、四楼窗网格边距、主体落位区间、投影底 725≤742。

## 5. 变更文件清单

1. `frontend/apps/operations-web/src/components/LoginBrandArt.vue`（能量线上移 55、场景 translate(-50,-20)、窗网格对称边距、注释更新含 P3-1 数字修正）
2. `.agents/frontend_experience_engineer.md`（基线同步：安全区 480-1020 左移版、禁贴地 22px 间距、能量线 ≥30 间隔、窗网格 ≥11 对称、竖屏取舍记载修正）

## 6. 未做范围与风险待决

- P3-3 右楼窗边距 1 单位不对称差（不可辨，不改）。
- v1 纪要登记的 P3-5（swatch 色值两处不一致）/P3-6（color-mix Safari 兼容、hover 对比）仍留在台账，未随本批处理。
- ICP 备案号/协议链接仍为占位（上线前替换，TODO 已有）。

## 7. 下一步骤入口

待用户在 http://localhost:5174/login 刷新验收四项调整效果；确认后按双库同步规则提交 Git/SVN。

> 本纪要为评审会签记录，不替代用户批准。
