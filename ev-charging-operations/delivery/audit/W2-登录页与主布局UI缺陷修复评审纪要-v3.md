# W2-登录页与主布局 UI 缺陷修复评审纪要 v3（三轮修复）

- **日期**：2026-08-21
- **评审子代理**：frontend_experience_engineer（只读会签，定义 `.agents/frontend_experience_engineer.md`）
- **评审对象**：用户 2026-08-21 三轮提报 2 项（v1/v2 纪要见同目录）
- **评审方式**：子代理只读评审＋数学验算＋curl 接口实测＋vue-tsc 复跑＋浏览器实测
- **结论**：**通过**——无 P0/P1/P2；4 项 P3（2 项措辞已随批修正、2 项登记台账）

## 1. 用户提报问题与根因

| # | 用户提报 | 根因（已确认） | 处置 |
|---|---------|--------------|------|
| 1 | 楼层外面/盖住外边框的小窗口删掉 | v-for="w in N" 从 1 起算，窗网格公式直接用 `w %`/`floor(w/…)` 导致行列分组错位：右楼1（w=16→y=756）、右楼2（w=6→y=748）各 1 窗完全悬浮楼外（楼底 740）、左楼2（w=6→y=732 底 745）1 窗盖住楼底边框、左楼1 末行仅 1 孤窗 | 公式改 `(w-1)` 分组＋y 起点垂直对称（上二轮引入、本轮修复） |
| 2 | 图形验证码不见了 | **后端 platform-service（8081）进程未运行**，/api/v1/auth/captcha 返回 HTTP 500 空体 → 前端 loadCaptcha 静默降级（captcha=null）→ v-if 不渲染图片。非前端代码 bug | 启动后端（java -jar，compose 中间件本就在运行） |

## 2. 修复实现（LoginBrandArt.vue）

四组窗公式统一改 `(w-1)` 分组，y 起点做垂直对称：

| 楼 | 公式 | 行×列 | 水平边距 | 垂直边距(顶/底) |
|---|------|-------|---------|----------------|
| 左楼1 | `221+((w-1)%4)*23`、`557+⌊(w-1)/4⌋*44` | 4×4=16 | 11/11 | 37/38 |
| 左楼2 | `347+((w-1)%2)*33`、`602+⌊(w-1)/2⌋*46` | 2×3=6 | 17/17 | 32/33 |
| 右楼1 | `1142+((w-1)%4)*26`、`561+⌊(w-1)/4⌋*48` | 4×4=16 | 12/11 | 21/22 |
| 右楼2 | `1274+((w-1)%2)*26`、`605+⌊(w-1)/2⌋*48` | 2×3=6 | 14/15 | 25/26 |

## 3. 验证证据

- **验证码**：浏览器实测 captchaLoaded=true（naturalWidth>0，data:image/png;base64）；点击刷新触发 GET /api/v1/auth/captcha 200 且换新图；curl 8081 直连返回 SUCCESS+base64 PNG。
- **窗户**：4 栋楼窗户数 16/6/16/6、每行 4/4/4/4 与 2/2/2、outOfBounds 全 0、底部边距 22-38；三主题截图无悬浮小方框、网格整齐。
- **typecheck**：vue-tsc 通过。
- **子代理独立验算**：四组公式行 y 值、楼内包含性、边距全部与实现吻合；注释 16 个数值与公式零偏差。

## 4. 问题分级与处置

| 级别 | 问题 | 处置 |
|------|------|------|
| P3-1 | 验证码加载失败无页内重试入口（v-if 使刷新按钮随图消失）；空体 500 时 api-client response.json() 抛异常经 void 调用形成 unhandled rejection | **登记台账**（基线已定调静默降级；建议后续 loadCaptcha catch＋「重新加载」占位） |
| P3-2 | 基线「水平对称边距」与右楼 12/11、14/15（整数网格固有差 1）文本不自洽 | **已修正**：基线改为「左右均衡、差 ≤1」 |
| P3-3 | 基线「右楼 2 窗」易误读为「右楼2」 | **已修正**：改为「右楼共 2 窗」 |
| P3-4 | W2 前端源码（components/stores/views/api/router）git 未跟踪，多轮修复无版本锚点 | **登记台账**：建议尽快初始提交建立回溯基线（待用户批准后执行） |

## 5. 变更文件清单

1. `frontend/apps/operations-web/src/components/LoginBrandArt.vue`（4 组窗公式 (w-1) 修正＋垂直对称＋注释）
2. `.agents/frontend_experience_engineer.md`（基线补垂直对称要求＋(w-1) 分组教训＋措辞修正）

## 6. 下一步骤入口

待用户刷新 http://localhost:5174/login 确认（窗户整齐、验证码显示）；确认后建议执行 W2 前端源码初始 git 提交（P3-4）并按双库同步规则提交。

> 本纪要为评审会签记录，不替代用户批准。
