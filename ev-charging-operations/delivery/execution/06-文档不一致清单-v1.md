# 文档不一致清单

> 版本：v1.0
> 状态：待用户审核（本轮只登记问题，不做任何修复；每项处理方式待拍板后执行）
> 生成时间：2026-08-19
> 背景：W2 Step 3/4 完成后进行代码结构调整（common 抽取共享库、service 接口/实现分离、域包结构落地、replaceRoles 乐观锁契约升级），全面核对 `docs/` 现役文档与代码/契约现状的差异。

## 核对基准（代码与契约现状）

1. 共享库：`backend/libraries/starter/`（Maven artifactId `evco-starter`）承载原 platform-service 的 `common` 包全部能力，包名 `com.elink.evco.web.*`，子包 `trace`/`error`/`security`/`config`/`util`；管理端鉴权由 `evco.web.security.enabled` 开关控制（默认 true，个人小程序等无权限服务置 false）。
2. platform-service 包结构：`com.elink.evco.platform.iam` 域包直挂（含 `controller`/`dto`/`vo`/`entity`/`mapper`/`service`/`service/impl`/`cache`/`util`），根包仅启动类 + `config`（PersistenceConfig）；无 `module/` 中间层、无 `common/`、无 `support/`。
3. service 全部接口化：接口在 `service` 包，实现类在 `service/impl` 子包（共 10 个）。
4. iam-v1.yaml 已升 v1.2.0：`ReplaceUserRolesRequest` 必填 `version`（乐观锁），409 `VERSION_CONFLICT`；`roleIds` 为雪花 ID 字符串数组（`items: type: string`）。
5. auth-v1.yaml 已升 v1.2.0：全部 200 响应补齐 ApiResponse 信封；`m1/wechat-mock` 标注交付批次依赖。

## 一、包结构类不一致（`module/<domain>` 中间层 vs 实际域包直挂）

| 编号 | 文档 | 位置 | 现有描述 | 实际现状 | 建议（待拍板） |
| --- | --- | --- | --- | --- | --- |
| A-1 | docs/standards/全栈研发工程规范-v1.md | L41-65 结构图、L200 测试路径 | 根包含 `module/<module>/`、`common/`、`support/`；测试镜像路径 `module/<module>/` | 域包直挂（如 `platform/iam/`）；`common` 已上移 starter；无 `support`；新增 `cache/`、`util/` 子包为规范未定义位置 | 更新结构图为域包直挂 + starter 分层说明，补 `cache`/`util` 包职责 |
| A-2 | docs/standards/文档与代码输出归档规则.md | L32-35 输出路径表 | `platform/module/` 下 `master`/`trade`/`operations`/`customer`/`iam`/`marketing` 模块 | 实际 `platform/iam/`（其余域未开工，开工时按同规则直挂） | 路径改为 `platform/<domain>/` |
| A-3 | docs/design/充电运营平台工程架构与数据分库设计-v1.md | L295 | "根包仅保留启动类、`config`、`common`、`support`，每个业务模块在 `module/<module>/` 内组织…" | 同 A-1；web 通用能力归 starter，服务根包仅启动类 + `config` | 更新为域包直挂 + starter 归属说明 |
| A-4 | docs/design/W1-工程跑道技术设计与契约包.md | L121 | "各服务 `module/<module>`" | 同 A-1 | 同步修正 |
| A-5 | docs/design/W2-IAM个人身份与配置技术设计与契约包.md | L283 | `module/iam/{controller,dto,vo,service,mapper,entity,convert,exception,validator,statemachine,policy}` | 实际 `iam/{controller,dto,vo,entity,mapper,service,service/impl,cache,util}`；未建 `convert`/`exception`/`validator`/`statemachine`/`policy`（当前规模未达到拆分阈值） | 对齐实际包清单；保留 validator/statemachine/policy 作为规模化后演进方向 |
| A-6 | docs/api/IAM用户与角色接口规范-v1.md | L57 | `module/iam/{controller,dto,vo,service,mapper,entity,convert,exception}` | 同 A-5 | 同 A-5 |

## 二、共享库 starter 未登记

| 编号 | 文档 | 位置 | 现有描述 | 实际现状 | 建议（待拍板） |
| --- | --- | --- | --- | --- | --- |
| B-1 | docs/standards/研发工程规范基线-v1.md | L24-35 目录所有权表 | `backend/libraries/` 仅列 `shared-kernel`、`contracts`、`testkit` | 新增 `starter`（evco-starter）：基础层（链路追踪、全局异常、MyBatis 通用拦截器）所有服务可用；管理端层（统一鉴权、两档权限拦截）由 `evco.web.security.enabled` 控制 | 目录表补 `backend/libraries/starter/` 行及可放/禁止内容 |
| B-2 | docs/standards/全栈研发工程规范-v1.md | L35、L44-45 | `common/` 描述为"仅服务内复用"；"跨服务能力放 shared-kernel" | web 通用组件已上移 starter 跨服务共享；服务内不再有 `common` 包 | 补 starter 分层规则：何时进 starter、何时进 shared-kernel、何时留服务内 |
| B-3 | docs/design/W1-工程跑道技术设计与契约包.md | L117-124 | 共享契约/测试工具（未含 starter） | starter 为 W2 期间新增共享库 | 登记该库的建立事实与职责边界 |

## 三、API 接口规范与契约版本差异

| 编号 | 文档 | 位置 | 现有描述 | 实际现状 | 建议（待拍板） |
| --- | --- | --- | --- | --- | --- |
| C-1 | docs/api/IAM用户与角色接口规范-v1.md | L3 | 版本 v1.1 | iam-v1.yaml 已至 v1.2.0 | 升版并补修订记录 |
| C-2 | 同上 | L36、L49 | `ReplaceUserRolesRequest` 仅 `roleIds`；接口表未标 409 | 契约 v1.2.0：必填 `version`（乐观锁）；并发替换返回 409 `VERSION_CONFLICT` | 接口表与字段表补 `version`，测试项补"角色替换乐观锁冲突" |
| C-3 | 同上 | L47、L49 | `roleIds` 类型 `array<number>` | 契约与 DTO 均为 `array<string>`（雪花 ID 统一序列化为字符串） | 类型改为 `array<string>` |
| C-4 | 同上 | L62-67 修订记录 | 止于 v1.1 | 缺 v1.2 修订条目 | 补记录 |
| C-5 | docs/api/认证与SSO接口规范-v1.md | L5 依据行 | 依据 auth-v1.yaml v1.1 | 契约已至 v1.2.0（响应信封补齐、m1/wechat-mock 批次标注） | 依据行与受影响章节同步 |
| C-6 | docs/api/ 目录整体 | — | 仅 3 份规范：通用、IAM、认证 | `contracts/openapi/` 另有 `channel-config`、`high-risk-operations`、`personal-identity`、`platform-foundation` 4 份契约无人读规范文档 | 待确认：是否按工程约定（API 规范必须存 docs/api/）补齐，或对纯公共组件/未开工契约豁免 |

## 四、其它待确认项

| 编号 | 事项 | 说明 | 建议（待拍板） |
| --- | --- | --- | --- |
| D-1 | 单域服务不按域分包的约定登记 | 用户已拍板"服务下只有一个域则不按域分包（服务名即域）"，目前仅存在于会话记忆，未写入任何工程规范文档 | 建议并入 A-1 修订时写入《全栈研发工程规范》 |
| D-2 | service 接口/impl 分离约定登记 | 已落地（10 个 service 全部接口化），《全栈研发工程规范》L52 已有 `service/impl` 描述，与现状一致，无需修改 | 无需处理（列此仅作核对结论） |
| D-3 | `文档导航与归档清单-v1.md` 登记 | 本清单与新增的前端对接速查文档需登记入口 | 文档创建后补登记 |

## 处理原则（待审核确认）

1. 本清单只登记、不修复；用户逐项拍板"修 / 不修 / 延后"后另行执行。
2. 涉及代码与文档方向性冲突的（A 类），默认建议以代码现状为准修文档（代码已过 32 项测试与 Checkstyle/ArchUnit 门禁）；若用户认为应保留 `module/` 中间层，则反向调整代码，回归量较大。
3. C 类属于契约先行、文档滞后，修复成本低、不影响运行时。

## 修订记录

| 时间 | 版本 | 原因 |
| --- | --- | --- |
| 2026-08-19 | v1.0 | W2 代码结构调整后全面核对 docs 现役文档，输出不一致清单待审核。 |
