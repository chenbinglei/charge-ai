spring:
  application:
    name: ecosystem-service
  main:
    banner-mode: "off"
server:
  port: ${EVCO_ECOSYSTEM_SERVER_PORT:8086}
  shutdown: graceful
management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      probes:
        enabled: true
  info:
    env:
      enabled: false
