spring:
  application:
    name: finance-service
  main:
    banner-mode: "off"
server:
  port: ${EVCO_FINANCE_SERVER_PORT:8082}
  shutdown: graceful
management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      probes:
        enabled: true
  info:
    env:
      enabled: false
